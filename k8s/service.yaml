apiVersion: v1
kind: Service

metadata:
  name: student-app-service

spec:
  type: NodePort

  selector:
    app: student-app

  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081
